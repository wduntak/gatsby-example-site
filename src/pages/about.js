import React from 'react';
import Layout from '../components/Layout';

export default function AboutPage(props) {
    return (
        <Layout>
            <p>I am on the About page!</p>
        </Layout>
   );
}